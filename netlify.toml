[build]
  publish = "public"
  command = "echo 'No build step needed'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  # Backend URL will be set in Netlify dashboard
  VITE_BACKEND_URL = "https://your-backend-url.herokuapp.com"

